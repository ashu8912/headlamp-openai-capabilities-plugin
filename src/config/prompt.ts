export const prompt = {
    "pod_error": "Hey i have this pod info in json tell me if there is something wrong with it point it out specifically and suggest fixes, try to answer it in an easy language and cut through extra stuff make it minimal The json starts here"
}